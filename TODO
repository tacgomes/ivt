TODO
====
* Support optional arguments (an argument can be or not given for some
  option):

  command --foo=[$foo]

* Add a test case for when an argument is not given for some option that
  requires one.

* Support mutually exclusive options (only one can be given):

  command --foo | --bar

* Support float ranges.

* Support validating positional arguments (arguments that are neither an
  option or an argument to an option).

* Support nested option declaration:

  command [ --foo=$foo [ --bar=$bar ] ]
